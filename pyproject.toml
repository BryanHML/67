[project]
name = "67"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bryan", email = "bryanho146@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mediapipe>=1.0.1",
    "opencv-python>=5.0.0.93",
]

[project.scripts]
67 = "67:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
